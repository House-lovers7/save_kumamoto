# Fresh-thread handoff（2026-08-01 10:20 更新／カード18件・No-Go #1 は熊本市の3件が未解消）

**再開方法**: 新セッションで `/resume-handoff /Users/tg/projects/app_development/save_kumamoto/HANDOFF.md`。
着手前に必ず `git log --pretty='%h %ad %s' --date=iso -5` で HEAD が `4143b07` 以降かを確認し、
先行完走している作業を二重実行しないこと。

## 次セッション冒頭でやること（時刻が近い順）

1. **`food-hikawa` は 2026-08-01 17:00 JST に失効する**。氷川町の水・食料配布は当日限りの
   告知なので、共通の24時間ではなく配布終了時刻で切ってある。翌日分の告知が出ていれば
   時間・場所・`expiresAt`・`verifyPoints` のラベルを当日分へ更新する。
   告知が消えていれば `unavailable` にするか、カードを外す判断が要る。
2. **他の全カードは 2026-08-02 09:30 JST に失効する**。公式14URL（重複除く。
   `docs/OPERATIONS.md` 第5章）を再確認し、`PATROL_AT` / `PATROL_EXPIRES_AT` を更新して
   `npm run gen:mobile-data` → `npm test` → commit。
   **再確認なしの期限延長は鮮度の捏造で禁止**。
3. **取得と時刻設定の間が空いたら取り直す**。この回は調査待ちで9時間空いたため、
   巡回の最後に全URLを取り直してから `PATROL_AT` を決めた。

## Goal

熊本の被災者が「いま何に困っているか」から公式情報へ最短で到達できる読み取り専用アプリ（R1）を、
Web とネイティブで同じ品質・同じ内容で提供する。情報がないときに「利用可能」と推測せず、
鮮度と失効を正直に出すことを最優先の品質基準とする。

## Decisions（確定済み。覆さないこと）

1. 停止フラグはサーバー側だけが読み、クライアントへは **props で渡す**
2. 環境変数名は **`EMERGENCY_MODE`**。`NEXT_PUBLIC_` は**絶対に付けない**
3. **infra を足さない**（KV / D1 / R2 / Durable Objects なし）
4. ネイティブの停止経路（EAS Update / OTA）は対象外。**ストア申請の必須前提条件**として文書化済み
5. 運用体制: **停止判断者・訂正担当者・問い合わせ対応は運営者1人が兼務** /
   窓口は **GitHub Issues** / 対応可能時間は **9:00〜21:00 JST ベストエフォート**
6. **巡回の判定は手順単位**。「そのページにカードの話題があるか」では緩い。
   **「カードの steps に書いた行動が1つずつリンク先で実行できるか」**で判定する。
   実行できなければ ①手順・説明を出典に合わせて書き直す ②実在ページへ導線を再選定する
   ③どちらもできなければ `sourceStatus: "unavailable"` にして `unverified` に何が無いかを書く
7. 誤認防止の表示に書けるのは、**出典ページに実際に書かれている記載だけ**。区分を推測で作らない
8. 出典ドメインの allowlist（`tests/data-contract.test.mjs` の `ALLOWED_SOURCE_HOSTS`）は
   **運営主体が公的機関だと確認できたものだけを個別に列挙**する。ワイルドカードにしない
9. **カードは畳んで出す**（2026-08-01 ユーザー確定）。畳んだ状態で残すのは、要約・有効期限・
   接続確認・「未確認」の警告・公式サイトへのボタン。手順・確認項目・注意・出典はタップで展開。
   Web は `<details>` を使う（条件レンダリングにすると誤認防止の表示が初期HTMLから消え、
   「JS が動く前でも出ている」という品質基準を壊すため）

## このセッション（2026-07-31 20:40〜2026-08-01 10:20）で完了したこと

| commit | 内容 |
|---|---|
| `86fec12` | ネイティブが `unavailable` のカードへ「公式情報」と出す誤認を止め、誤認防止3表示を移植 |
| `5c5aa77` | 誤認防止の表示が Web とネイティブ両方にあることをソース検査で固定 |
| `1144ad6` | 8月1日の巡回。氷川町の水・食料配布を新規に見つけ、4カード追加 |
| `515c884` | トップを1画面に収め、カードを既定で畳む |
| `adc3ce1` | ネイティブのタップ領域を実測し、48dp を割っていた3種を修正 |
| `4143b07` | 巡回結果とタップ領域実測を運用文書へ反映 |

### No-Go #1 の進展［高］

**氷川町で食料配布が公式に出ていた**。「8月1日（土曜日）の水・食料品の配布について」
（7/31 19:00 更新。9:00〜11:00 と 15:00〜17:00、竜北西部小学校・竜北東小学校・氷川中学校）。
探し続けていた「受け取る側の場所と時間」が公式に出ている唯一の例。

**熊本市には無い**。冊子PDFの目次まで当たった結果、生活面への支援に炊き出し・食品の給与に
あたる項目そのものが無く、「被服、寝具その他生活必需品の支給」は申請受付前、支給品一覧も
前回災害時の内容と明記されている。区役所5区は共通記事のみ、上下水道局の緊急情報20件にも
物資・乳幼児・し尿は0件。宇城市・宇土市・八代市も該当なし（`docs/OPERATIONS.md` 第5章に一覧）。

### 追加した4カード

- `food-hikawa` 氷川町の水・食料配布（**当日限りの告知。個別 `expiresAt`**）
- `shelter-hikawa` 氷川町の開設中の避難所
- `bath-kumamoto` 熊本市の無料入浴サービス（8月1日開始）
- `toilet-container` 国交省が派遣したコンテナ型トイレ（美里町・宇城市・氷川町・八代市）。
  **熊本市は派遣先に含まれないので `toilet` の空白は埋まらない**

### 出典を超えていた表示の修正

`evidence` の手順「建物の外側4方向を撮影する」は出典のどこにも書かれていなかった
（出典は「家の外と中」「部屋ごとの全景」「被災箇所の寄り」）。7/31 の `shelter` と同種の、
`unavailable` 表示より危険な誤認。あわせて `water` で取りこぼしていた2点
（「飲料用」でも検査次第で飲用不可になりうる／管理企業へ直接電話しない）を追加した。

### 検証結果［高］

| 検証 | 結果 |
|---|---|
| `npm run lint` | PASS |
| `npm test` | **26件 PASS**（誤認防止のパリティ検査を1件追加） |
| `npm run gen:mobile-data` 後の `git status` | 差分なし（18カード） |
| mobile `npm run typecheck` / `npm run lint` | PASS |
| 出典URLの実到達 | 全件 HTTP 200（14件＋新規） |
| タップ領域（Web） | 390/360/320px すべて基準未満0件・横スクロールなし |
| タップ領域（ネイティブ） | **44dp未満 10件 → 0件**（Androidエミュレータで実測） |
| ネイティブ実描画 | Pixel_8_API_35 で誤認防止表示・カード開閉・個別期限を目視確認 |

## Scope（残り）

### A. No-Go #1 の残り3カード（熊本市）

`food-and-supplies` / `toilet` / `infant-care`。**あたって無かった先は `docs/OPERATIONS.md`
第5章に2つの表で列挙済み**なので、同じ場所を再探索しないこと。まだ当たっていない先:

- 熊本県の令和8年熊本地震ページの下位ページ（今回は市中心に見た）
- 熊本市の各区役所の「福祉課」個別ページ（区トップは共通記事だった）
- 上下水道局の通常記事が404から復活したか（毎回確認する）

### B. No-Go #2 を `verified` にする（Human Approval Gate の先）

`docs/OPERATIONS.md` 第6章のチェックリスト 5〜9 が未達。**着手前に潰す前提が2つある**。

1. **停止手順がそのままでは動かない可能性**［中］。リポジトリのルートに `wrangler.toml` /
   `wrangler.jsonc` が無く、Worker 設定は `dist/server/wrangler.json`（Worker 名
   `kumamoto-action-navigator-web`）にしかない。`npx wrangler secret put EMERGENCY_MODE` は
   `--config` か `--name` の指定が要ると見ている。**緊急時に最初に叩く手順なので、
   本番デプロイの承認と同時に確定させること**（`docs/OPERATIONS.md` 3.2 に注記済み）
2. **デプロイコマンドは `npx vinext deploy`**。`wrangler deploy` ではない。`npm run` に
   デプロイ用スクリプトは無い
3. **問い合わせ窓口 URL の確定**。現在 **remote が未設定**で GitHub Issues が存在しない
4. **本番 Workers での実操作**（デプロイ承認後）: secret 設定 → 別端末で確認 →
   反映実時間を記録 → 停止中デプロイで維持されるか → secret 削除
5. **ネイティブに停止が届かないことの受諾**（運営者の明示的な同意）

### C. その他

1. **iOS**: `npx expo run:ios` が xcodebuild error 65。`expo-modules-jsi` の `weak let`
   （Swift 6.3 / SE-0481）で、手元は Swift 6.2。**Xcode 26.4+ への更新が必須**
   （App Store 経由・ユーザー操作）。更新後は
   `rm -rf apps/mobile/node_modules/expo-modules-jsi/apple/Products` してから
   `cd apps/mobile && npx expo run:ios --device "iPhone 16"`。
   `nonisolated(unsafe) weak var` 回避策はメンテナが非推奨なので採用しない
2. **Android の残課題**: TalkBack の実発話、**物理実機**（今回はエミュレータのみ）、
   60秒 tick の境界跨ぎ。注意: TalkBack を有効化したら必ず
   `adb shell settings delete secure enabled_accessibility_services` で戻す
3. **初期転送量の再計測**。約101KB は 2026-07-30 の値で、以後 UI を追加・削除している
4. **キャラクター／アイコン**（くまモンは No-Go 確定）。困りごとグリッドからは漢字1文字を
   外したので、アイコンを入れるならこの位置。119/110 の導線の隣にマスコットを置かない

## 次回のカード追加候補（実在と内容は確認済み。追加判断は未実施）

1. **熊本市の応急給水所**［高］。上下水道局が毎日更新。現行の `water`（井戸水提供）とは
   別の情報で、**断水している人が最初に必要とするのはこちら**
2. **熊本市の診療可能な医療機関一覧**［中］。`city.kumamoto.jp/kiji00345259/index.html`。
   県全域をカバーしないため県全域カードの出典にはできない
3. **県のペット同行避難が可能な避難所**［中］。`pref.kumamoto.jp/soshiki/30/275292.html`
4. **八代市の応急給水**［中］。今回の調査で存在を確認（食料配布は無い）

## 運用メモ（次回の巡回で時間を節約するため）

- **熊本市上下水道局の緊急情報は静的HTMLに出ない**。実体は
  `https://www.kumamoto-waterworks.jp/list.php` の JSON で、
  **`X-Requested-With: XMLHttpRequest` と `Referer` を付けないと空**が返る
- `curl` は hook でブロックされるため node の fetch を使う（ユーザー承認済みの代替手段）
- `meti.go.jp` 系は WebFetch が 403 になる。node の fetch に UA を付ければ 200
- **PDF は `pdftotext -layout` で読める**（`/opt/homebrew/bin/pdftotext`）。市の冊子や
  国交省の記者発表はPDFなので、本文確認にはこれが要る
- **ネイティブの実描画確認**: `~/Library/Android/sdk/emulator/emulator -avd Pixel_8_API_35` →
  `cd apps/mobile && npx expo run:android`（Metro が起動したままになるので後で止める）→
  `python3 scripts/qa/ui.py text|shot|tap`、`python3 scripts/qa/tap_targets.py`
- **タップ領域の測定は画面内に入れてから**。uiautomator の bounds は画面外をクリップするため、
  画面下端や横スクロールの端にある要素は実寸より小さく出る（幅1.9dp の「避難所」など）
- **Web の実測**: `npx vinext start -p 3100`（3000 は Gitea が使用中）＋ headless Chrome の
  `--remote-debugging-port=9333` → `node scripts/qa/tap-target-check.mjs 9333 <url> <width>`

## 未検証のまま残っていること（推測で埋めない）

- **本番 Cloudflare Workers での反映実時間**。デプロイ承認後にしか測れない
- **`npx wrangler secret put` が設定ファイル無しで動くか**（上記 Scope B-1）
- **停止中デプロイで停止が維持されるか**。コードからの推論［高］だが本番未実施
- **実利用者への到達遅延**。開いたままのタブ・オフラインPWA・ネイティブへは届かない
- **iOS の実描画**（Xcode 更新待ち）と **Android 物理実機**
- **TalkBack / VoiceOver の実発話**
- **住民拠点SS検索の地図に熊本県内のSSが実際に何件入っているか**［中］
- **コンテナ型トイレが現在も設置されているか**。7月30日の派遣発表しか公式情報が無く、
  各市町のサイトには掲載がない（カードでは向かう前の電話確認を手順に入れている）

## 実戦投入できるかの評価

**まだできない。** 判定は **`implementation_complete_boundary_unverified`** のまま。

止めているのは4つ。

1. **No-Go #1 が未解消**（Scope A）。熊本市の3カードが「公式の案内を確認できていません」表示
2. **本番境界**（Scope B）。デプロイ承認、問い合わせ窓口 URL、停止手順の確定
3. **毎日巡回の継続**。24時間期限のため、巡回が止まった時点で全件が失効する。
   氷川町の配布カードは当日17:00で失効する
4. **実機・支援技術の未検証**（Scope C）。エミュレータでの実描画までは確認済み

このほか README「公開前に残る課題」の現地評価（3〜5人）・署名境界は未着手のまま。
