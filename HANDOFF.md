# Fresh-thread handoff（2026-08-01 14:02 更新／カード19件・No-Go #1 は熊本市の3件が未解消）

**再開方法**: 新セッションで `/resume-handoff /Users/tg/projects/app_development/save_kumamoto/HANDOFF.md`。
着手前に必ず `git log --pretty='%h %ad %s' --date=iso -5` で HEAD が `a67facb` 以降かを確認し、
先行完走している作業を二重実行しないこと。

## 直近のセッション（14:02）で確認したこと

No-Go #1 の未探索3導線を読んだが、熊本市の `food-and-supplies` / `toilet` / `infant-care` を
公式情報で埋められる受け取り先は見つからなかった。

- 県「防災情報くまもと」: 避難所・道路・平時ハンドブック中心。受け取る側の配布先は未確認
- 熊本市「窓口開庁について」: 分類ページのみ。物資・トイレ・乳幼児の個別案内は未確認
- 熊本市「義援物資について」: 個人の受入れは行わず、企業・団体の提供を事前登録で受ける案内
- 上下水道局: 「災害対応中のため緊急情報のみ掲載」が継続。通常記事は未復活

ポータルは動的画面のため「情報が存在しない」とまでは断定しないが、同じ導線を再探索しない。
詳細は `docs/OPERATIONS.md` 第5章の14:02追記。No-Go #1 は未解消のまま。

## 直近のセッション（13:35〜13:52）で完了したこと

**巡回差分スクリプトを実装し、実URLで検証まで完了した。再実装しないこと。**

| commit | 内容 |
|---|---|
| `29c5c76` | RED: HTML/JSON正規化、重複排除、期限切れ、HTTP失敗、レポートのテスト契約 |
| `1d8712c` | `scripts/qa/patrol-diff.mjs` の実装。終了コード0/1/2、fixture、JSON出力 |
| `872e157` | `npm run patrol:diff` と運用手順を追加。support-systemsの3行を公式本文の表現へ揃える |
| `644decf` | 上下水道局の2段取得とfetch例外の回帰テスト |

- **実境界**: 2026-08-01 13:48 JST、19カード・17 `sourceUrl` を取得。17 URLすべて取得成功、
  `facts.items` / `facts.citedAs` / `sourceLandmark` は要確認0件
- **`npm test` 39件 PASS**／Web typecheck・lint PASS／mobile typecheck・lint PASS
- `support-systems` の3行は情報内容を変えず、公式本文に実在する語順へ揃えた。
  Web正典からmobileデータも再生成済み
- **これは11:25巡回の更新ではない。`PATROL_AT` は11:25のまま**。
  `[到達]` のカードは文字列照合対象が無く、stepsの意味確認・`publishedAt`確認も未実施。
  再確認なしで鮮度時刻を進めないこと
- 通常実行: `npm run patrol:diff`。終了コード0=取得・引用一致、1=要確認、2=取得失敗。
  0でも人間による手順単位の確認は必要（`docs/OPERATIONS.md` 第5章）

## 直近のセッション（12:35〜13:40）で完了したこと

**UX改修「カードを答えを先に見せる面へ寄せる」は実装・検証まで完了した。**
下記「新規ユーザー要望」の設計方針は消化済み。次セッションで再着手する必要はない。

| commit | 内容 |
|---|---|
| `332e933` | `facts` / `sourceLandmark` / `visibleFacts()` を追加し、10カードへ出典の答えを写す |
| `a67facb` | OPERATIONS に facts 照合手順、DESIGN にデータ構造、docs/qa にリンク深さの実測 |

- **`npm test` 31件 PASS**（26→31）／`npm run lint` PASS／mobile typecheck・lint PASS
- **`PATROL_AT` は 11:25 のまま**。12:40〜13:00 の取得は巡回ではない（OPERATIONS 第5章に明記）
- **17:00 / 19:00 の失効対応と翌 11:25 の巡回は未実施のまま**（下記のとおり時刻待ち）

### この改修で巡回の作業が増えている［高］

`facts` の各行は毎日の巡回で出典と1行ずつ照合する対象になった。手順は
`docs/OPERATIONS.md` 第5章「カード内の答え（`facts`）の照合」。
特に `dated: true` の3か所（`water-station` の給水所12か所／`food-hikawa` の配布時間・場所／
`evidence` の8月1日・2日の臨時窓口）は**当日分への入れ替えが必要**。

## ユーザーからの指摘（2026-08-01 13:50）— 次セッションの最優先［高］

**「アプリとしてなりに問題がある。プロにできなきゃ意味がない。早く作りたかったのに
なんでこんな時間かかってるん」**

### 1. 「アプリとしての問題」の内容は**未特定**。推測で直さない

何を指しているかを本人に確認してから着手する。推測で直すと、今日と同じく時間だけ溶ける。
聞くべきこと: どの画面で / 何をしようとして / 何が起きたか。

### 2. 速度が出ていない原因（実測にもとづく）

今日の80分の内訳は **調査・計画25分 / 実装25分 / 検証30分**。
検証30分は Android エミュレータが Metro へ到達できず**成果ゼロ**だった。
10分で切り上げて「未検証」と記録すべきだった（判断ミス）。

構造的な原因はこちら。**1回の変更が遅くなる設計になっている**。

| 遅さの原因 | 実態 | 効く手 |
|---|---|---|
| 出典の文言を手で写す | カード1枚ごとに公式ページを開いて目視。今日は8ページ | **巡回スクリプト**: 17URLを取得し、カードの `facts` / `citedAs` / `sourceLandmark` の各文字列が今も本文にあるかを機械で差分表示する。判断は人、照合は機械 |
| 毎日の巡回が全手作業 | 17URL × 19カード。巡回が止まると全件失効 | 同上。差分が出たカードだけ人が読む形にする |
| ネイティブ実描画の確認 | エミュレータ起動＋ビルドで毎回20〜30分。今日は失敗 | **リリース前だけに限定**し、日常の変更は Web の実測だけで回す |
| カード枚数の増加 | 19枚。巡回コストは枚数に比例 | 枚数を増やす判断のたびに巡回コストを見積もる |

**巡回スクリプトは13:52に実装・実URL検証まで完了した**（`scripts/qa/patrol-diff.mjs`）。
以降は `npm run patrol:diff` で差分を絞り、差分カードと `[到達]` カードだけを人が判断する。

## 次セッション冒頭でやること（時刻が近い順）

1. **`food-hikawa` は 2026-08-01 17:00 JST に失効する**。氷川町 `kinkyu.html` で
   8月2日分の配布告知を確認する。出ていれば時間・場所・`expiresAt`・`verifyPoints`・
   **`facts`**（時間と場所3か所）・**`sourceLandmark`**（見出し文言）を当日分へ更新、
   出ていなければ `unavailable` にするかカードを外す判断が要る。
   8/1分の告知は前日19:00に出た（翌日分も前日夕方に出る可能性がある）。
   **12:50 時点では8月2日分は未掲載を確認済み**。
2. **`water-station` は 2026-08-01 19:00 JST に失効する**。上下水道局 `list.php` で
   8月2日分の応急給水所の告知を確認する。8/1分は「前日19:00の第18報が初出 →
   当朝07:00の第21報が再掲」というパターンだったので、**17:00 のセッションで
   氷川町と一緒に翌日分を確認するのが効率的**。拠点は日ごとに減っていく傾向
   （7/31で5か所終了）。**`facts` の12か所（学校名・住所）と `sourceLandmark` の
   報番号も当日分へ入れ替える**。12:45 時点では第21報（8月1日分）のままを確認済み。
3. **他の全カードは 2026-08-02 11:25 JST に失効する**。公式**17**URL（重複除く。
   `docs/OPERATIONS.md` 第5章。support-systems の出典差し替えで1件増えた）を再確認し、
   `PATROL_AT` / `PATROL_EXPIRES_AT` を更新して
   `npm run gen:mobile-data` → `npm test` → commit。
   **再確認なしの期限延長は鮮度の捏造で禁止**。
   **`facts` の各行の照合も必須**（OPERATIONS 第5章の新しい手順）。
   あわせて `publishedAt` の更新日追従も行う（12:55 の取得で kiji00372143 /
   kiji00372079 / kiji00372200 の最終更新日が 8月1日へ進んでいるのを確認済み。
   鮮度を前進させないため 11:25 の値のまま据え置いてある）。
4. **取得と時刻設定の間が空いたら取り直す**。巡回の最後に全URLを取り直してから
   `PATROL_AT` を決める（今回もこの手順で 11:25 に確定した）。

## Goal

熊本の被災者が「いま何に困っているか」から公式情報へ最短で到達できる読み取り専用アプリ（R1）を、
Web とネイティブで同じ品質・同じ内容で提供する。情報がないときに「利用可能」と推測せず、
鮮度と失効を正直に出すことを最優先の品質基準とする。

## 新規ユーザー要望（2026-08-01 12時ごろ・音声。次セッションで設計反映）

**「リンク先に飛んだところで情報が複雑でわからない。欲しい情報へ直接ダイレクトに
飛んでほしい」「欲しい情報をまとめたところに飛ばないと、そもそも情報を探しきれない。
複雑だ」**（要望原文の趣旨。2回の発言）。

含意: 公式サイト側に「まとまった場所」があることを期待できない以上、
**アプリ自身が「欲しい情報がまとまっている場所」になる**。リンクは出典明示と
最新確認のためであって、ユーザーに公式サイトの構造を探索させるためではない。

**この設計方針は 2026-08-01 13:40 時点で実装・検証まで完了している**（commit `332e933` /
`a67facb`）。以下は判断の記録として残す。実施結果は `docs/qa/link-depth-2026-08-01/README.md`。

設計方針（Fable 判断。実装・カード単位の判断は次セッション）:

1. **カードを「リンク集」から「答えを先に見せる面」へ寄せる**。出典に書かれている
   核心情報（今日の時間・場所・住所・電話番号）はカード内に直接出し、リンクは
   「最新確認・裏取り」の位置づけにする。food-hikawa / water-station / bath-kumamoto は
   既にこの形なので、**残りのカードを同じ水準へ引き上げる**。Decision #7
   （出典に実際に書かれている記載だけ）はそのまま維持する。
2. **全19カードの sourceUrl の深さを再点検する**。判定基準は「タップして最初に見える
   画面に、カードが約束した情報があるか」。より深い安定URL（個別記事・実在アンカー）が
   あれば再選定する。集約ページ（list04828）へ飛ばすカードが第一候補。
3. **深いURLが存在しない出典**（上下水道局トップ=JSONの表示面、氷川町 kinkyu.html=
   1ページに全告知が縦積み）は、steps に**ページ内の実在の見出し文言を名指し**で書く
   （例:「『8月1日の応急給水所について』の見出しを探す」）。見出し文言は出典の実物から
   引用し、巡回のたびに照合する。
4. **受入条件**: 各カードが次のいずれかを満たす。
   ①カード内で答えが読める ②リンク先の最初の画面で答えが見える
   ③steps がページ内の実在見出しを名指ししている。
   実測（タップ→目的情報まで何スクロールか）を docs/qa に記録する。
5. **残リスク**: 深い個別URLほど災害対応中に404になりやすい（水道局の通常記事で実証済み）。
   再選定したURLは巡回対象一覧（OPERATIONS 第5章）の更新と、毎日の死活確認を伴う。
   リンクを深くするより、カード内へ核心情報を出す方が404リスクを増やさない。

**優先順位**: 17:00 / 19:00 の失効対応と明日 11:25 の巡回が先。このUX改修は
その後に着手する（巡回と同時に行うと、内容更新と構造変更の差分が混ざるため分ける）。

**実施結果（2026-08-01 13:40）**: 12:35 時点で 17:00 まで4.5時間あり、巡回が来ていない
時間帯だったため先にUX改修を入れ切った（ユーザー確定）。失効対応は別コミットに分ける。

- 方針1（答えを先に見せる）: **完了**。`facts` として10カードへ出典の記載を写した
- 方針2（sourceUrl の深さ再点検）: **完了**。差し替えたのは `support-systems` の1件のみ
  （集約ページは JS のページ送りで目的記事が DOM に無く到達不能だった）。
  `shelter` は実体が別ドメイン（salesforce-sites.com）のため **allowlist 判断待ちで保留**
- 方針3（実在見出しの名指し）: **完了**。`sourceLandmark` を5カードへ
- 方針4（受入条件と実測）: **完了**。`docs/qa/link-depth-2026-08-01/README.md`
- 方針5（残リスク）: 深い個別URLは `kiji00372143` の1件だけ増やし、巡回対象へ追加した

## Decisions（確定済み。覆さないこと）

1. 停止フラグはサーバー側だけが読み、クライアントへは **props で渡す**
2. 環境変数名は **`EMERGENCY_MODE`**。`NEXT_PUBLIC_` は**絶対に付けない**
3. **infra を足さない**（KV / D1 / R2 / Durable Objects なし）
4. ネイティブの停止経路（EAS Update / OTA）は対象外。**ストア申請の必須前提条件**として文書化済み
5. 運用体制: **停止判断者・訂正担当者・問い合わせ対応は運営者1人が兼務** /
   窓口は **GitHub Issues** / 対応可能時間は **9:00〜21:00 JST ベストエフォート**
6. **巡回の判定は手順単位**。「カードの steps に書いた行動が1つずつリンク先で実行できるか」で
   判定する。実行できなければ ①手順・説明を出典に合わせて書き直す ②実在ページへ導線を
   再選定する ③どちらもできなければ `sourceStatus: "unavailable"` にして `unverified` に
   何が無いかを書く
7. 誤認防止の表示に書けるのは、**出典ページに実際に書かれている記載だけ**。区分を推測で作らない
8. 出典ドメインの allowlist（`tests/data-contract.test.mjs` の `ALLOWED_SOURCE_HOSTS`）は
   **運営主体が公的機関だと確認できたものだけを個別に列挙**する。ワイルドカードにしない
9. **カードは畳んで出す**（2026-08-01 ユーザー確定）。畳んだ状態で残すのは、要約・有効期限・
   接続確認・「未確認」の警告・公式サイトへのボタン。Web は `<details>` を使う

## このセッション（2026-08-01 10:35〜11:40）で完了したこと

| commit | 内容 |
|---|---|
| `c42d1aa` | allowlist に熊本市上下水道局（kumamoto-waterworks.jp）を追加。運営者表記を確認済み |
| `5e27805` | 11:25 の巡回。応急給水所カード（water-station）を追加、8月7日期限の記載を出典に合わせて下ろす |
| （このcommitの後） | docs: OPERATIONS 第5章と本 handoff の更新 |

### 11:25 巡回の要点［高］

- **熊本市の応急給水所を新カード `water-station` として追加**。上下水道局の第21報
  （8月1日07:00時点）: 8:00〜19:00、南区5・西区1・北区6の12か所（住所つき）。
  日付つき告知なので `food-hikawa` と同じ**当日終了時刻（19:00）の個別 `expiresAt`**。
  断水者が最初に必要とするのは井戸水（water）よりこちら。
- **出典が更新日表示を変えずに本文を変える事例を確認**。「住家の緊急の修理」
  （kiji00372143）の完了期限「令和8年8月7日までに完了」が「災害発生の日から10日以内です」へ
  差し替わっていた（最終更新日表示は 7/31 のまま）。support-systems から具体日を下ろした。
  **カードが引用している文言は毎回本文で照合すること**（更新日比較では検出できない）。
- **Scope A（熊本市3カードの未探索先）は3か所とも該当なしで消化**:
  県の地震ハブ（soshiki/1/274517.html）全リンク／熊本市5区の「健康・福祉」カテゴリ／
  上下水道局の通常記事（トップに「災害対応中のため緊急情報のみ掲載しています」と明記、
  復活していない）。集約ページの全42件（ページ送り `hpkijilistpagerhandler.ashx` で直接取得）も
  受け取る側向けの配布記事なし。**No-Go #1 は未解消のまま**。
- 熊本市の新着: 「災害見舞金等の支給」「臨時窓口開庁」（8/1。金銭・窓口の案内で、
  食料・物資の配布ではない。見舞金記事の本文は未読）。

### 検証結果［高］

| 検証 | 結果 |
|---|---|
| `npm test` | **26件 PASS**（19カード。verifyPoints の「区別は2択以上」契約に water-station も適合） |
| `npm run gen:mobile-data` 後の `git status` | 差分はコミット済み（19カード生成） |
| `npm run lint`（tsc） | PASS |
| mobile `npm run typecheck` / `npm run lint` | PASS |
| 出典URLの実到達 | **17件すべて HTTP 200**（16URL + list.php、11:25 の最終取り直し） |

### デプロイ状況（ユーザー質問「デプロイできてる？」への回答記録）［高］

**未デプロイ。** Cloudflare API で Worker `kumamoto-action-navigator-web` が
アカウント（Cokomo.gt@gmail.com / 66896ac6…）上に**存在しない**ことを確認（code 10007）。
今日 10:57 に `dist/` の再ビルドの形跡があるが、wrangler の deploy 実行ログは無い
（ビルドのみでデプロイ未到達とみられる。誰が実行したかは未確認）。

## Scope（残り）

### A-0. `shelter` の出典差し替えの可否（**運営者の判断が要る**）［高］

2026-08-01 12:55 に発見した。熊本市トップの「避難所情報」の実体は
**熊本市防災情報ポータル**（`https://city-kumamoto.my.salesforce-sites.com/`）で、
開設状況・所在地・避難世帯数・混雑状況つきの避難所一覧が**サーバー側で描画**されている
（システム更新時間 2026/8/1 11:14、実データを取得して確認済み）。

`shelter` カードの出典をここへ差し替えれば、タップした最初の画面に答えが出る。
いまは市トップのままで `sourceLandmark: "避難所情報"` を出しているだけ。

**判断が要る理由**: `salesforce-sites.com` は Salesforce の共有ドメインで、
出典 allowlist（Decision #8「運営主体が公的機関だと確認できたものだけを個別に列挙」）へ
`city-kumamoto.my.salesforce-sites.com` を足す判断になる。判断材料:

- 熊本市公式サイト（`city.kumamoto.jp`）のトップから「熊本市防災情報ポータル」として
  リンクされている
- ページタイトルが「熊本市防災情報ポータル」、フッターに「熊本市防災公式X」への導線
- ドメイン自体は SaaS 事業者のもので、`.jp` でも `.go.jp` でもない

**承認されるまで差し替えない**（データ側にもコメントで固定してある）。

### A. No-Go #1 の残り3カード（熊本市）

`food-and-supplies` / `toilet` / `infant-care`。**あたって無かった先は `docs/OPERATIONS.md`
第5章の3つの表で列挙済み**（09:30 の2表 + 11:25 の1表）。同じ場所を再探索しないこと。
2026-08-01 14:02 に上記3導線を確認済み。いずれも受け取る側の案内は無く、
同じ導線を再探索しない。上下水道局トップだけは、毎回の巡回で
「災害対応中のため緊急情報のみ掲載しています」の文言が消えたかを確認する。

### B. No-Go #2 を `verified` にする（Human Approval Gate の先）

`docs/OPERATIONS.md` 第6章のチェックリスト 5〜9 が未達。**着手前に潰す前提が2つある**。

1. **停止手順がそのままでは動かない可能性**［中］。ルートに `wrangler.toml` が無く、
   Worker 設定は `dist/server/wrangler.json` にしかない。`npx wrangler secret put EMERGENCY_MODE`
   は `--config` か `--name` の指定が要ると見ている。**本番デプロイの承認と同時に確定させること**
2. **デプロイコマンドは `npx vinext deploy`**。`wrangler deploy` ではない
3. **問い合わせ窓口 URL の確定**。remote 未設定で GitHub Issues が存在しない
4. **本番 Workers での実操作**（デプロイ承認後）: secret 設定 → 別端末で確認 →
   反映実時間を記録 → 停止中デプロイで維持されるか → secret 削除
5. **ネイティブに停止が届かないことの受諾**（運営者の明示的な同意）

### C. その他

1. **iOS**: `npx expo run:ios` が xcodebuild error 65。**Xcode 26.4+ への更新が必須**
   （App Store 経由・ユーザー操作）。更新後は
   `rm -rf apps/mobile/node_modules/expo-modules-jsi/apple/Products` してから
   `cd apps/mobile && npx expo run:ios --device "iPhone 16"`
2. **Android の残課題**: TalkBack の実発話、**物理実機**、60秒 tick の境界跨ぎ。
   TalkBack を有効化したら必ず
   `adb shell settings delete secure enabled_accessibility_services` で戻す
3. **初期転送量の再計測**（約101KB は 2026-07-30 の値。以後 UI とカードが増えている）
4. **キャラクター／アイコン**（くまモンは No-Go 確定）。119/110 の導線の隣にマスコットを置かない

## 次回のカード追加候補（追加判断は未実施）

1. **熊本市の診療可能な医療機関一覧**［中］。`city.kumamoto.jp/kiji00345259/index.html`。
   県全域をカバーしないため県全域カードの出典にはできない（内容確認済み）
2. **県のペット同行避難が可能な避難所**［中］。`pref.kumamoto.jp/soshiki/30/275292.html`
   （内容確認済み。氷川町の緊急情報にも県の犬猫相談ダイヤルが出ている）
3. **八代市の応急給水**［中］。存在確認済み（食料配布は無い）。allowlist 追加が必要
4. **県「要配慮者等への宿泊施設提供及び被災者への入浴支援事業」**［低］。
   `pref.kumamoto.jp/soshiki/45/244416.html`。県ハブでリンク実在のみ確認、**本文未読**。
   熊本市の入浴カード（bath-kumamoto）は市限定なので、県全域を埋められるかも
5. **県「クーリングシェルター」**［低］。`pref.kumamoto.jp/soshiki/49/275092.html`。
   リンク実在のみ確認、**本文未読**

## 運用メモ（次回の巡回で時間を節約するため）

- **熊本市上下水道局の緊急情報は静的HTMLに出ない**。実体は
  `https://www.kumamoto-waterworks.jp/list.php` の JSON で、
  **`X-Requested-With: XMLHttpRequest` と `Referer` を付けないと空**が返る。
  市民向けの表示面はトップページ（water-station カードの sourceUrl）
- **熊本市の list ページの「もっと見る」の続きは直接取得できる**:
  `https://www.city.kumamoto.jp/dynamic/hpkiji/pub/hpkijilistpagerhandler.ashx?c_id=3&class_id=4828&class_set_id=1&pg=2&kbn=kijilist&top_id=0`（pg を増やす。全42件なら pg=4 まで）
- `curl` は hook でブロックされるため node の fetch を使う（ユーザー承認済みの代替手段）。
  scratchpad の `fetch.mjs` パターン: meti系はUA付与、waterworks は上記ヘッダ
- **PDF は `pdftotext -layout` で読める**（`/opt/homebrew/bin/pdftotext`）
- **ネイティブの実描画確認**: `~/Library/Android/sdk/emulator/emulator -avd Pixel_8_API_35` →
  `cd apps/mobile && npx expo run:android` → `python3 scripts/qa/ui.py text|shot|tap`、
  `python3 scripts/qa/tap_targets.py`
- **Web の実測**: `npx vinext start -p 3100`（3000 は Gitea が使用中）＋ headless Chrome の
  `--remote-debugging-port=9333` → `node scripts/qa/tap-target-check.mjs 9333 <url> <width>`
- **区役所5区の「健康・福祉」カテゴリURL**: chuo/list00145・higashi/list00205・
  nishi/list00275・minami/list00335・kita/list00415（今回すべて該当なしを確認済み）

## 未検証のまま残っていること（推測で埋めない）

- **本番 Cloudflare Workers での反映実時間**。デプロイ承認後にしか測れない
- **`npx wrangler secret put` が設定ファイル無しで動くか**（Scope B-1）
- **停止中デプロイで停止が維持されるか**。コードからの推論［高］だが本番未実施
- **実利用者への到達遅延**。開いたままのタブ・オフラインPWA・ネイティブへは届かない
- **iOS の実描画**（Xcode 更新待ち）と **Android 物理実機**
- **TalkBack / VoiceOver の実発話**
- **water-station カードのネイティブ実描画**（エミュレータ確認は前回18カード時点。
  今回はテスト・typecheck までで、実描画は未確認）
- **`facts` / `sourceLandmark` のネイティブ実描画**。13:20〜13:45 にエミュレータで試みたが
  **アプリが Metro へ到達できず失敗**（エミュレータ側から `10.0.2.2:8081` が
  `Network is unreachable`。バンドル自体はホストから6.4MB/6秒で取得できる）。
  この改修とは無関係の実行環境の問題。再試行はエミュレータの**コールドブート**
  （`-no-snapshot-load`）から。詳細と切り分けは `docs/qa/link-depth-2026-08-01/README.md`
- **`facts` の一覧開閉のネイティブのタップ領域**（Web は4件とも実測 48dp）
- **住民拠点SS検索の地図に熊本県内のSSが実際に何件入っているか**［中］
- **コンテナ型トイレが現在も設置されているか**。7月30日の派遣発表（第2報）しか公式情報が無い
- **翌日分の応急給水所・氷川町配布の告知が何時に出るか**。8/1分の実績
  （前日19:00・当朝07:00）しかない
- **10:57 の dist/ 再ビルドを誰が実行したか**

## 実戦投入できるかの評価

**まだできない。** 判定は **`implementation_complete_boundary_unverified`** のまま。

止めているのは4つ。

1. **No-Go #1 が未解消**（Scope A）。熊本市の3カードが「公式の案内を確認できていません」表示
2. **本番境界**（Scope B）。デプロイ承認、問い合わせ窓口 URL、停止手順の確定。
   **本日時点で Worker は未デプロイ**（アカウント上に存在しないことを確認済み）
3. **毎日巡回の継続**。24時間期限のため、巡回が止まった時点で全件が失効する。
   当日限りの2カードは 17:00（氷川町）と 19:00（応急給水所）に失効する
4. **実機・支援技術の未検証**（Scope C）。エミュレータでの実描画は18カード時点まで確認済み

このほか README「公開前に残る課題」の現地評価（3〜5人）・署名境界は未着手のまま。
