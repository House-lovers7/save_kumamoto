# Fresh-thread handoff（2026-07-31 20:40 更新／No-Go #1 は 4件 → 3件）

**再開方法**: 新セッションで `/resume-handoff /Users/tg/projects/app_development/save_kumamoto/HANDOFF.md`。
次の担当は実装・検証モデル（sonnet 想定）。設計判断は確定済みなので、上位モデルへ戻す必要はない。
着手前に必ず `git log --pretty='%h %ad %s' --date=iso -5` で HEAD が `a5a8264` 以降かを確認し、
先行完走している作業を二重実行しないこと。

**次セッション冒頭でやること**: 鮮度の毎日巡回。全カードの期限は **2026-08-01 20:16 JST** に切れる。
それまでに公式12URL（重複除く。`docs/OPERATIONS.md` 第5章）を再確認し、`lib/disaster-data.ts` の
`PATROL_AT` / `PATROL_EXPIRES_AT` を更新して `npm run gen:mobile-data` → `npm test` → commit。
**再確認なしの期限延長は鮮度の捏造で禁止**。

## Goal

熊本の被災者が「いま何に困っているか」から公式情報へ最短で到達できる読み取り専用アプリ（R1）を、
Web とネイティブで同じ品質・同じ内容で提供する。情報がないときに「利用可能」と推測せず、
鮮度と失効を正直に出すことを最優先の品質基準とする。

**No-Go #2（訂正・停止体制）の Web 側は完了**（コード・回帰テスト・実測証拠・運用文書）。
**No-Go #1 は未解消**。14カード中3カードで公式の該当案内を確認できず `unavailable`。

## Decisions（確定済み。覆さないこと）

1. 停止フラグはサーバー側だけが読み、クライアントへは **props で渡す**
2. 環境変数名は **`EMERGENCY_MODE`**。`NEXT_PUBLIC_` は**絶対に付けない**
3. **infra を足さない**（KV / D1 / R2 / Durable Objects なし）
4. ネイティブの停止経路（EAS Update / OTA）は対象外。**ストア申請の必須前提条件**として文書化済み
5. 運用体制（2026-07-31 ユーザー確定）: **停止判断者・訂正担当者・問い合わせ対応は運営者1人が兼務** /
   窓口は **GitHub Issues** / 対応可能時間は **9:00〜21:00 JST ベストエフォート**
6. **巡回の判定は手順単位**（2026-07-31 20:16 更新）。「そのページにカードの話題があるか」では
   まだ緩い。**「カードの steps に書いた行動が1つずつリンク先で実行できるか」**で判定する。
   実行できなければ、①手順・説明を出典に合わせて書き直す ②実在ページへ導線を再選定する
   ③どちらもできなければ `sourceStatus: "unavailable"` にして `unverified` に何が無いかを書く
7. 誤認防止の表示に書けるのは、**出典ページに実際に書かれている記載だけ**。区分を推測で作らない
8. 出典ドメインの allowlist（`tests/data-contract.test.mjs` の `ALLOWED_SOURCE_HOSTS`）は
   **運営主体が公的機関だと確認できたものだけを個別に列挙**する。ワイルドカードにしない
   （2026-07-31 ユーザー確定。国の公式ドメインも個別追加なら可）

## このセッション（2026-07-31 19:50〜20:40）で完了したこと

| commit | 内容 |
|---|---|
| `244f282` | 出典ドメイン allowlist を `ALLOWED_SOURCE_HOSTS` へ切り出し、資源エネルギー庁を追加 |
| `a5a8264` | 20:16 の巡回結果。fuel の導線を新規発見、破綻4件の手順を実態へ修正 |

### 巡回で判明したこと［高］

**18:30 の巡回でもまだ緩かった**。「そのページにカードの話題があるか」までは見ていたが、
「カードの手順が約束していることができるか」までは見ていなかった。手順1つずつを突き合わせた結果、
掲載継続としていた8件のうち**4件で手順が実行できなかった**。

| カード | 実行できなかった手順 | 処置 |
|---|---|---|
| medical | 公式の医療情報で診療状況を確認する | 手順を「保険証がなくても受診できることを確認する」（出典の実記載）へ差し替え |
| shelter | ペット・車中泊・車椅子などの条件を確認する | 説明文が出典に無い条件一覧を約束していた。行く前に電話で確認する形へ修正 |
| communication | 携帯会社の障害情報／00000JAPAN／公衆電話 | 実際にある171・各社の災害用伝言板・JAPANローミングへ寄せ、注意書きから 00000JAPAN を外した |
| roads | 公共交通の運行を確認する | 導線をトップから地震対応の本文ページへ寄せ、通行止めと迂回路を見る形へ |

`shelter` の「ペット同伴、車中泊、車椅子、乳幼児、充電などの利用条件を確認します」は、
**`unavailable` 表示より危険な誤認**だった。出典に無いものを説明文が約束していたため。

### 解消した1件

**fuel**: 資源エネルギー庁「住民拠点SS等検索」（`https://www.enecho-ss.meti.go.jp/b/enecho/`）。
震度5強以上の地震発生時に給油所の営業状況を都道府県単位で地図表示する公式システムで、
令和8年熊本地震（震度7）が「現在の災害情報」として稼働中。
**営業可／営業不可／確認中**の3区分は出典が実際に使っている区分なので `verifyPoints` にした。
配送可否の正規化（前セッションで裏取りできず未実装）に対して、**初めて裏取りできた区分**。

### 検証結果［高］

| 検証 | 結果 |
|---|---|
| `npm run lint` | PASS |
| `npm test` | **25件 PASS** |
| `npm run gen:mobile-data` 後の `git status` | 差分なし |
| 出典URLの実到達（重複除く12件） | **全件 HTTP 200** |

タップ領域は再測定していない。今回の変更で押せる要素の数・配置は変わっておらず
（`verifyPoints` は表示ブロックの追加のみ）、前回実測（390/360/320px で基準未満0件）が有効［中］。

## Scope（残り）

### A. No-Go #1 の残り3カード

`food-and-supplies` / `toilet` / `infant-care`。**当たって無かった先は `docs/OPERATIONS.md`
第5章に列挙済み**なので、同じ場所を再探索しないこと。次に当たる先の候補:

- **各市町村の個別ページ**（宇城市・宇土市・八代市・氷川町）は今回も未調査。
  allowlist の追加判断が要る（Decisions 8）
- **熊本市の各区役所**（食料・生活用品の配布は区単位で出ている可能性）［低］
- トイレは、熊本市上下水道局が災害対応を終えて通常サイトへ戻れば
  「マンホールトイレ・貯水機能付給水管のある避難場所」記事（現在404）が復活する可能性［中］

### B. No-Go #2 を `verified` にする（Human Approval Gate の先）

`docs/OPERATIONS.md` 第6章のチェックリスト 5〜9 が未達。

1. **本番 Workers での実操作**（デプロイ承認後）
   - `npx wrangler secret put EMERGENCY_MODE`（値 `true`）
   - **別端末**で `/` からカードが消え `/status` が「緊急縮退中」になることを確認
   - **反映までの実時間を記録**する
   - 停止中に無害な変更をデプロイし、停止が維持されることを確認
   - `npx wrangler secret delete EMERGENCY_MODE` で解除
2. **問い合わせ窓口 URL の確定**。現在 **remote が未設定**で GitHub Issues が存在しない
3. **ネイティブに停止が届かないことの受諾**（運営者の明示的な同意）

### C. ネイティブのパリティ差分（今回さらに悪化した）

`apps/mobile/src/app/index.tsx` には `verifyPoints` / `irreversibleOrder` / `unverified` の
描画コードが**1箇所も無い**。データは生成物として配られているのに Web だけが誤認防止表示を持つ。

さらに `index.tsx:383` 付近は `sourceStatus` を見ずに**全カードへ「公式情報」タグを固定表示**して
いるため、**`unavailable` の3カードがネイティブでは「公式情報」と表示される**。
今回 fuel に `verifyPoints`（営業可／営業不可／確認中）を足したので、ネイティブだけが
「確認中」の意味を出せない状態にもなった。**単なる未実装ではなく、誤認を生む表示**。

`apps/mobile/` のコードは Non-Scope のままなので、着手には解除の判断が要る。

### D. その他（前セッションから継続）

1. **iOS**: `npx expo run:ios` が xcodebuild error 65。`expo-modules-jsi` の `weak let`
   （Swift 6.3 / SE-0481）で、手元は Swift 6.2。**Xcode 26.4+ への更新が必須**
   （App Store 経由・ユーザー操作）。ディスクは確保済み（26GB → 45GB）。
   更新後は `rm -rf apps/mobile/node_modules/expo-modules-jsi/apple/Products` してから
   `cd apps/mobile && npx expo run:ios --device "iPhone 16"`。
   `nonisolated(unsafe) weak var` 回避策はメンテナが非推奨なので採用しない
2. **キャラクター／アイコン**（くまモンは No-Go 確定）。困りごとグリッドは現状 漢字1文字。
   119/110 の導線の隣にマスコットを置かない
3. **Android の残課題**: TalkBack の実発話、物理実機、60秒 tick の境界跨ぎ。
   注意: TalkBack を有効化したら必ず `adb shell settings delete secure enabled_accessibility_services` で戻す
4. **初期転送量の再計測**。約101KB は 2026-07-30 の値で、以後 UI を追加している

## 次回のカード追加候補（今回の巡回で見つけたが、スコープ外にしたもの）

いずれも**実在と内容を確認済み**。カードを増やす判断は未実施。

1. **熊本市の応急給水所**［高］。熊本市上下水道局が毎日更新している。8月1日分は
   南区5・西区1・北区6の計12か所、8:00〜19:00、容器持参の依頼つき。断水地区の一覧もある。
   現行の `water` カード（熊本市の井戸水提供・72箇所）とは別の情報で、
   **断水している人が最初に必要とするのはこちら**
2. **熊本市の診療可能な医療機関一覧**［中］。`city.kumamoto.jp/kiji00345259/index.html` から
   市医師会のページで検索できる。県全域をカバーしないため県全域カードの出典にはできず、
   熊本市向けの別カードにするなら追加できる
3. **県のペット同行避難が可能な避難所**［中］。`pref.kumamoto.jp/soshiki/30/275292.html`
   （2026-07-31 更新）。熊本市2施設・八代市2施設・宇城市は調整中

## 運用メモ（次回の巡回で時間を節約するため）

- **熊本市上下水道局の緊急情報は静的HTMLに出ない**。トップは「緊急情報データ取得中・・・」の
  プレースホルダで、実体は `https://www.kumamoto-waterworks.jp/list.php` の JSON。
  **`X-Requested-With: XMLHttpRequest` と `Referer` を付けないと空**（1バイト）が返る。
  実ブラウザを起動しなくても node の fetch で中身を確認できる
- **Chrome 拡張（claude-in-chrome）は今回未接続**だった。実ブラウザが要るときは
  `scripts/qa/*.mjs` が使う headless Chrome + CDP の経路を先に検討する
- `curl` は hook でブロックされるため node の fetch を使う（ユーザー承認済みの代替手段）
- `meti.go.jp` 系は WebFetch が 403 になる。node の fetch に UA を付ければ 200

## 未検証のまま残っていること（推測で埋めない）

- **本番 Cloudflare Workers での反映実時間**。デプロイ承認後にしか測れない
- **停止中デプロイで停止が維持されるか**。`wrangler deploy` が vars を全消しし secrets は残す、
  というコードからの推論［高］だが本番未実施
- **実利用者への到達遅延**。開いたままのタブ・オフラインPWA・ネイティブへは届かない（設計上の既知の穴）
- `Cache-Control` 未設定の HTML がエッジ・中間プロキシで実際にキャッシュされるか［中］
- **住民拠点SS検索の地図に、熊本県内の各SSの営業状況が実際に何件入っているか**［中］。
  災害情報の稼働と3区分の絞り込みUIは確認したが、地図データは JS が別途取得するため
  件数までは見ていない

## 受領済みで未処理のニーズ観測（30件超）

2026-07-31 受領。大半は「給水車が今いるか」「冷房が動いているか」といった**現在の稼働状況**を求めており、
自治体・現場からの更新経路がなければ原理的に出せない。

- **実装済み**: 給水地点の用途と水質検査状態の強制表示（`verifyPoints`）、
  支援制度の期限と手続き順序（`irreversibleOrder`）、
  給油所の営業状況の3区分（`verifyPoints`。2026-07-31 20:16 追加）
- **裏取りできず未実装**: 配送可否の正規化（公式に該当区分の記載が無い）
- **運用連携が前提で今は着手すべきでない**: 給水車のリアルタイム在否、避難所の暑熱ステータス、
  要配慮者の移送調整、災害ごみの搬出支援受付、支援者のQR認証

一般利用者の投稿だけで「利用可能」と判定しない、という現行の制約は全案で維持する。

## 実戦投入できるかの評価

**まだできない。** 判定は **`implementation_complete_boundary_unverified`** のまま。

止めているのは4つ。

1. **No-Go #1 が未解消**（Scope A）。14カード中3カードが「公式の案内を確認できていません」表示
2. **本番境界**（Scope B）。デプロイ承認と問い合わせ窓口 URL の確定
3. **毎日巡回の継続**。24時間期限のため、巡回が止まった時点で全件が失効する
4. **ネイティブのパリティ差分**（Scope C）。誤認防止表示が Web にしかなく、
   さらにネイティブは `unavailable` のカードに「公式情報」タグを出している

このほか README「公開前に残る課題」の現地評価（3〜5人）・ネイティブ実機・署名境界は未着手のまま。
