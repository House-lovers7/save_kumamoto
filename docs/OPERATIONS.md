# 運用手順（訂正・停止）

作成日：2026年7月31日

誤った案内を出してしまったときに、**誰が・どうやって・どこまで**止められるかを定義する。
`docs/RELEASE_AUDIT.md`「公開停止条件」の #2（運営・訂正体制）と #3（数分以内の停止手段とrollback）に対応する（旧「README.md の No-Go #2」。2026-08-02 に実体が `docs/RELEASE_AUDIT.md` へ移ったため参照を付け替えた）。

**対象は Web のみ。** iOS / Android のネイティブアプリには停止経路が一切ない（第3章の到達範囲表を参照）。

## 凡例

| 記号 | 意味 |
|---|---|
| `[要記入]` | **実在の値がまだ決まっていない。** 埋めるまで公開しない。推測で埋めてはいけない |
| `[提案]` | この文書からの提案。運営者が承認するまで確定ではない |
| `[高]` `[中]` `[低]` | 確度。`[高]`=このリポジトリのコード・コマンド出力・公式資料で直接確認済み |

---

## 1. 位置づけ

このアプリは公式サービスではなく、有志が公式情報への導線をまとめたもの。したがって
**間違いは「起きうる」前提**で運用する。守るべき順序は次のとおり。

1. 命に関わる誤り（誤った避難先、存在しない給水地点、間違った電話番号）を**止める**
2. 止めたことを利用者に**わかる形で**示す（黙って消さない）
3. 正しい情報へ**訂正**する
4. 訂正できないなら「現在の状況は確認できません」と**正直に出す**

「見えなくする」ことと「直す」ことは別。止めるのは即時、直すのは確認が取れてから。

---

## 2. 役割と連絡体制

現在は**運営者1人がすべてを兼務する**。将来分割できるよう役割は分けて書くが、
今の担当者列はすべて同じ人物を指す。

| 役割 | やること | 現在の担当 |
|---|---|---|
| 停止判断者 | 縮退モードを立てる／解除するかを決める | `House-lovers7`（GitHubアカウント名。運営者本人。本名の掲出はしない） |
| 訂正担当者 | `lib/disaster-data.ts` を直し、再生成・検証・デプロイする | 同上 |
| 問い合わせ対応 | 外部からの誤り報告を受け、重大度を判定する | 同上 |

`[要記入]` に入れる表示名について: 問い合わせ窓口を GitHub Issues にする以上、
**GitHub アカウント名が事実上の公開名**になる（コミット履歴にも表示される）。
本名を出すかどうかは運営者が決める。

### 単一障害点であること［高］

1人兼務なので、その人が対応できない間は**誰も止められない**。これは受け入れる前提であって、
解決済みの問題ではない。部分的なフェイルセーフは1つだけある。

- 13カードは `expiresAt`（`lib/disaster-data.ts` の `contentTimes`）を共有し、**24時間で一斉に失効**する
  （2026-07-31 ユーザー確定、第5章参照）。medical カード1件のみ個別の `staleMedicalTimes` を使う。
  失効すると各カードは「現在の状況は確認できません」へ切り替わり、行動手順と公式リンクだけが残る
- ただしこれは**最大24時間かかる**。今まさに危険な誤案内には無力。停止スイッチの代わりにはならない

### 問い合わせ窓口

| 項目 | 値 |
|---|---|
| 経路 | **GitHub Issues（このリポジトリ）** |
| URL | **https://github.com/House-lovers7/save_kumamoto/issues** ［高］2026-08-01 開設。public repo・Issues有効を `gh api` で確認済み |
| 対応可能時間 | **9:00〜21:00 JST**、ベストエフォート（応答時間は約束しない） |

正直に書いておくべきこと:

- GitHub Issues は**被災者本人にはほぼ使えない**。GitHub アカウントが要るうえ、
  停電・低速回線の下で開く導線ではない。実質的には支援者・自治体職員・開発者向けの経路
- 夜間（21:00〜翌9:00）に届いた誤り報告は**翌朝まで残る**。この時間帯に危険な誤りが出た場合、
  頼れるのは `expiresAt` の失効（確認から24時間）だけになる。24時間期限では夜間中に
  失効しないこともあるため、夜間の危険な誤りは自動では守れない
- `[提案]` 被災者からの導線が必要になったら、`/status` にメールアドレスか
  フォームURLを出す（第7章）。実値は `[要記入]`

---

## 3. 停止のランブック

### 3.1 重大度の分類

| 区分 | 例 | 対応 | 所要 |
|---|---|---|---|
| **A：即時縮退** | 誤った避難先、存在しない給水地点、間違った緊急連絡先、公式が否定した情報 | 縮退モードを立てる（全カードを止める） | 数分 |
| **B：当該カードのみ訂正** | 1件の受付時間・場所・URL の誤り。他カードは正しい | 縮退せず訂正だけデプロイ | 数十分 |
| **C：次回定時** | 表現の分かりにくさ、優先順位、誤字 | 通常の更新に含める | 次回 |

**迷ったら A**。カードを止めても 119・110 と自治体公式への導線は残るので、
止めることによる損失は「案内が見えなくなる」だけ。誤案内を出し続ける損失の方が大きい。

### 3.2 止める（区分A）

**再ビルド・再デプロイは不要。環境変数を1つ設定するだけ**［高］。

```bash
npx wrangler secret put EMERGENCY_MODE
# 値の入力を求められたら true と入れる
```

> **未検証の注意（2026-08-01）**［中］: このコマンドは**そのままでは動かない可能性がある**。
> リポジトリのルートに `wrangler.toml` / `wrangler.jsonc` が無く、Worker 設定は
> ビルド生成物の `dist/server/wrangler.json`（Worker 名 `kumamoto-action-navigator-web`）に
> しか無い。wrangler が設定を見つけられず、`--config dist/server/wrangler.json` か
> `--name kumamoto-action-navigator-web` の指定が要ると見ている。
> **緊急時に最初に叩く手順なので、本番デプロイの承認と同時に実行して確定させること。**
> あわせて、デプロイコマンドは `wrangler deploy` ではなく **`npx vinext deploy`**（`vinext` が
> wrangler をラップしている）。`npm run` にデプロイ用スクリプトは無い。

1. 上のコマンドを実行する
2. `/status` を開き、**「緊急縮退中」**と表示されることを確認する
3. `/` を**別の端末または別ブラウザ**で開き、個別カードが消えていることを確認する
   （自分の端末だけだとキャッシュを見ている可能性がある）
4. 止めた事実・時刻・理由を記録する

**なぜ Secret なのか**［高］: `wrangler deploy` は既定で **vars を全消ししてから設定ファイルの
vars を入れ直す**。`dist/server/wrangler.json` の `vars` は `{}` なので、ダッシュボードで
plain text var として設定した `EMERGENCY_MODE` は**次のデプロイで消える**。
停止中に誰かが文言修正をデプロイした瞬間に案内が復活する、という事故が起きる。
secrets はデプロイで消えず、かつ `process.env` に載る。
どうしても plain var で運用するなら、**すべてのデプロイで `--keep-vars` を付ける**。

### 3.3 解除する

```bash
npx wrangler secret delete EMERGENCY_MODE
```

`/status` が「通常表示」に戻ることを確認する。secret は読み出せないため、
**`/status` が現在の停止状態を知る唯一の経路**になる。

### 3.4 停止が届く範囲［高］

止めても全員に届くわけではない。これを理解せずに「止めた」と言わない。

| 対象 | 届くか | 遅延 |
|---|---|---|
| オンライン利用者の次のページ遷移・再読込 | **届く** | 次のナビゲーションまで（`public/sw.js` は navigation を network-first で取りに行く） |
| 開きっぱなしのタブ | **届かない** | 利用者が再読込するまで（ポーリング未実装） |
| オフラインの PWA 利用者 | **届かない** | 再接続してナビゲーションするまで（`sw.js` がキャッシュ済みの `/` を返す） |
| iOS / Android ネイティブ | **一切届かない** | ストア審査を通した新バージョンの配布まで（数日〜） |

ネイティブに届かない理由［高］: `expo-updates` を入れておらず、`app.json` に `updates` /
`runtimeVersion` の設定もない。カードデータはアプリに同梱されており、起動時に何も取りに行かない
（通信ゼロ設計）。これは privacy 上の判断の裏返しであって、バグではない。
**この非対称を運営者が受諾するまで、ストア申請はしない。**

### 3.5 停止スイッチ ≠ 失効表示

混同しやすいので分けて書く。

- **停止スイッチ**（`EMERGENCY_MODE`）: 運営者が意図して止める。サーバー側で判定するので
  ネイティブとオフライン利用者には届かない
- **失効表示**（`isExpired`）: `expiresAt` を過ぎたカードを自動的に「現在の状況は確認できません」へ
  切り替える。**端末の時計で動く**ので、オフラインでもネイティブでも効く

つまり失効表示は、停止が届かない利用者に対する**唯一の自動的な劣化通知**。
だから `expiresAt` を安易に延ばしてはいけない。

### 3.6 変えてはいけないもの［高］

停止スイッチが黙って効かなくなる変更。

- `nodejs_compat` を外す / `compatibility_date` を `2025-04-01` より前へ下げる
  → Workers が bindings を `process.env` へ流し込まなくなる（`vite.config.ts` で固定済み）
- 環境変数名に `NEXT_PUBLIC_` を付ける
  → vinext がビルド時に rsc/ssr を含む全環境へ define するため、値がサーバー側へ焼き付き、
  以後どの環境変数を変えても止まらなくなる
- `vinext build --prerender-all` を使う → `/` が静的化されると環境変数が効かない
- クライアントコンポーネントから停止フラグを読む → クライアントバンドルでは
  `process.env` が `{}` に置換され常に false になる（**これが 2026-07-31 に直した不具合そのもの**）

上の4つは `npm test` の `tests/emergency-mode.test.mjs` が機械的に検出する。
テストを消したり緩めたりしないこと。

---

## 4. 訂正のランブック（区分B）

```bash
# 1. 正典を直す（Web とネイティブの唯一の情報源）
#    lib/disaster-data.ts

# 2. ネイティブ用データを再生成する（忘れると Web とネイティブで内容がずれる）
npm run gen:mobile-data

# 3. 検証
npm run lint && npm test

# 4. デプロイ（Human Approval Gate。承認を得てから）
```

`npm test` には「正典を編集して再生成し忘れていない」という検査が入っている。
`git status` に `apps/mobile/src/data/actions.ts` の差分が残っていたら、手順2を飛ばしている。

### 直せる範囲の非対称［高］

| | Web | ネイティブ |
|---|---|---|
| 誤りを訂正する | デプロイで即時 | **ストア審査を通すまで不可（数日〜）** |
| 案内を止める | 環境変数で数分 | **不可** |

したがって**ネイティブに載せる情報は、Webよりも保守的にする**。
「24時間で変わる可能性がある値」をネイティブへ同梱するのは、そもそも設計が合っていない。

---

## 5. 情報の鮮度運用

### 巡回の判定ルール［高］

**リンクが生きていることと、そのページにカードの話題があることは別**。
リンク生存と「内容が変わっていない」ことだけを見る確認では、リンクが生きたまま
中身だけ話題を失う壊れ方を検出できない。判定はこの順で行う。

```text
1. リンクを開く
2. カードの steps に書いた行動が、そのページで実行できるか
   → できる  : sourceStatus は official のまま。checkedAt を更新する
   → できない: 同じ話題の実在ページを探す
       → 見つかった  : 導線を再選定し、カード文言を実態に合わせる
       → 見つからない: sourceStatus を "unavailable" にし、
                       unverified に「何が確認できていないか」を書く
```

`unavailable` にしたカードも `checkedAt` は更新してよい。
「確認できないことを、その時刻に確認した」という事実は正しいため。

### 現状［高］

#### 2026-08-05 02:36 JST の巡回（3日ぶり。5カード更新＋1カード新設）［高］

**巡回が 2026-08-01 11:25 から3日止まっていた。** その間も出典は動き続けており、
`water-station` は終了した給水所へ人を向かわせる内容のまま残っていた。
未公開だったため実害は出ていないが、**公開後に同じ間隔が空けば、そのまま無駄足になる**。

`PATROL_AT` は **02:36**。作業は 8月4日21:02 に始めたが、編集中に出典が動いたため
（下記「同じ晩に2度動いた出典」）、最後に全URLを取り直して差分ゼロを確認した時刻を採った
（「取得と時刻設定の間が空いたときの扱い」に従う）。
最終的な `npm run patrol:diff` は **20カード・17URL / 取得失敗 0 / 要確認 0（exit 0）**。
処置した差分は次のとおり。

| 出典 | 見つかった変化 | 処置 |
|---|---|---|
| 上下水道局 `list.php` | 第21報（8/1・12か所）→ **第36報（8月4日18:15時点）**。対象は8月5日で南区1か所（火の君文化センター）のみ。豊田小・西里小は8月4日12時で終了。第34報で**熊本市の断水は全て解消** | `water-station` を第36報へ更新。`expiresAt` を 8/5 19:00、`availableWindows` を 8/5 8:00〜19:00 へ。21:02 の時点では告知の対象日が翌日だったため枠を外していたが、02:36 に日付が揃ったので戻した |
| 氷川町 `kinkyu.html` | 「8月1日（土曜日）の水・食料品の配布について」が**消滅**。代わりに「支援物資のご協力をお願いします（8月4日19時時点）」＝**提供する側**への依頼 | `food-hikawa` を `sourceStatus: "unavailable"` へ。`facts`・`sourceLandmark`・`verifyPoints`・`availableWindows` を削除 |
| 氷川町 `kinkyu.html` | 「給水車による応急給水について」（8/1 15:36）は**継続**。時間7:00〜19:00／氷川町役場・宮原振興局／1回5ℓまで。氷川町は8月4日19時時点でも断水中 | **`water-hikawa` カードを新設**（食料の配布と給水は別の問いなので分ける） |
| 無料入浴（kiji00372200） | 最終更新日が 8/1 → **8月2日**。実施施設が12→**23か所**へ増加し、**店休日が新規掲載** | 23施設を区ごとの `facts` 4群へ（1群15件の上限があるため）。**大福湯の所在地の誤りを訂正**（`docs/FIXES.md`） |
| り災証明（kiji0032451） | 最終更新日が 8/1 → **8月2日**。8月1日・2日の臨時窓口が消滅。電子申請は受付開始済み、窓口は7/30から、受付は午前9時〜午後4時（月〜金・祝日除く） | `evidence` の `facts[0]` を常設の受付案内へ差し替え。申請窓口に**7つの総合出張所**を追加。「原則、被災後1月以内」は現存を確認し据え置き |
| 住家の緊急の修理（kiji00372143） | 最終更新日が 8/1 → 8/3 →（同じ晩に）**8月4日**。「災害発生の日から10日以内」が消滅。21:02 時点で追記されていた「同年8月27日まで延長する方向で、国（内閣府）と協議予定です（7/31時点）」は **02:36 の再照合では削除**。上限の記述も「（上限56,400円まで）を熊本市から修理業者へ支払う」へ変化 | `support-systems` から「10日以内」と延長協議の記述を削除。**8月7日の期限を延びるものとして扱わない**。上限の文言も現行へ |

**巡回で分かった構造上の問題（処置済み）**: 挙動テスト13件が 2026-08-01 のデータを固定具に
していたため、データを当日分へ進めた瞬間に落ちた。**巡回のたびにテストを直す運用は、
巡回を止める方向にしか働かない。** 合成カード（`tests/fixtures/cards.mjs`）へ移し、
正典データ側の約束（枠は当日・最終枠の end＝`expiresAt` など）は全カードに対する検査として
そのまま残した。以後の巡回でこれらのテストは壊れない。

**同じ晩に2度動いた出典［高］**: `kiji00372143`（住家の緊急の修理）は 21:02 と 02:36 の
5時間半のあいだに本文が書き換わり、延長協議の一文が消えた。**「今日はもう見た」で
済ませられる出典ではない。** 編集に時間がかかった巡回では、時刻を設定する前に必ず
全URLを取り直す（`npm run patrol:diff` の再実行で足りる）。

**取得失敗は一過性のことがある［中］**: 02:33 の実行で `medical`（HTTP 400）と
`communication`（fetch failed）の2URLが落ちたが、再実行では両方とも取得できた。
exit 2 が出たら**巡回成功として扱わず**、再実行して切り分ける。

**環境の時計に注意［高］**: この巡回の直前、シェルの `date` が実時刻より約13時間前を
返していた。出典側に「まだ存在しないはずの時刻の告知」が見えたことで気づき、
`date` / node / 出典サーバの HTTP `Date` ヘッダの3点照合で実時刻を確定させた。
**`PATROL_AT` は環境の時計をそのまま信じて書かない。**

#### 2026-08-01 12:40〜13:00 の取得（巡回ではない）［高］

UX改修（カード内に答えを出す）のために出典を取得した。**`PATROL_AT` は 11:25 のまま
前進させていない**（取得と鮮度表示の時刻をずらす場合は、実際より新しく見せない側へ倒す）。
このとき見つけた出典側の変化は次のとおりで、カード文言だけ追従させた。

| 出典 | 見つかった変化 | 処置 |
|---|---|---|
| 住家の緊急の修理（kiji00372143） | 最終更新日が 7/31 → **2026年8月1日**。「【完了期限のお知らせ】→災害発生の日から10日以内です」と「【完了期限】→ 令和8年（2026年）8 月7日までに完了」を**両方**掲載 | 11:25 に下ろした「8月7日」を戻した。`irreversibleOrder` と `facts` の両方 |
| 災害ごみ（kiji00372079） | 最終更新日が 7/31 → **8月1日** | 内容は同じ。問い合わせ先6件を `facts` へ |
| 無料入浴（kiji00372200） | 最終更新日が 7/31 → **8月1日**。実施施設12件が本文にも掲載（従来はPDF） | 12施設を `facts` へ |
| 上下水道局 `list.php` | **第21報（8月1日分）のまま**。8月2日分は未掲載 | 変更なし。17:00/19:00 に再確認 |
| 氷川町 `kinkyu.html` | **8月1日分のまま**。8月2日分は未掲載 | 変更なし。17:00 に再確認 |

**次の巡回（17:00・19:00・翌 11:25）では、`publishedAt` の更新日追従もあわせて行う。**

#### 2026-08-01 11:25 JST の巡回

判定は手順単位。カードは1件増えて19件になった。

| 結果 | 件数 | カード |
|---|---|---|
| **掲載継続**（手順もそのまま実行できる） | 13 | official-kumamoto / kumamoto-city-hub / water / food-hikawa / fuel / toilet-container / bath-kumamoto / elder-care / shelter / shelter-hikawa / medical / communication / roads |
| **新規追加** | 1 | water-station（熊本市の応急給水所。下記） |
| **出典の記載変更に追従** | 2 | support-systems（下記）/ evidence（更新日が8/1へ） |
| **`unavailable`** | 3 | food-and-supplies / toilet / infant-care（toilet は publishedAt を出典表示の 7/31 へ訂正） |

#### 11:25 の巡回の発見［高］

- **熊本市の応急給水所が公式に出ている**。上下水道局の緊急情報「【第21報】8月1日の
  応急給水所について（8月1日07:00時点）」に、当日 8:00〜19:00・南区5か所・西区1か所・
  北区6か所（学校名・住所つき）と、7月31日で終了した5か所が明記されている。
  日付つきの告知なので `water-station` カードとして追加し、`food-hikawa` と同じく
  当日の給水終了時刻（19:00）で失効させる。
- **出典が更新日表示を変えずに本文を変えることがある**。「住家の緊急の修理」
  （kiji00372143）の完了期限が「令和8年（2026年）8月7日までに完了」から
  「災害発生の日から10日以内です」へ差し替わっていたが、最終更新日表示は 7/31 のまま。
  更新日表示の比較だけでは記載変更を検出できないため、カードが引用している文言は
  毎回本文で確認する。support-systems の具体日はカードから下ろした。
- **上下水道局トップは「災害対応中のため緊急情報のみ掲載しています」と明記**。
  マンホールトイレ・応急給水の通常記事が404のままである根拠として、URLの死活ではなく
  サイト自身の宣言で確認できるようになった。
- 集約ページ（list04828）の一覧はJSの「もっと見る」で続きを読む構造（全42件）。
  続きは `hpkijilistpagerhandler.ashx?...&pg=2&kbn=kijilist` で直接取得できる。
  「地震被害に伴う災害ごみの収集等について」は2ページ目に掲載継続。

#### 2026-08-01 昼に追加であたった先（すべて該当なし。同じ場所を再探索しない）［高］

| あたった先 | 結果 |
|---|---|
| 県「令和8年熊本地震に関する情報」ハブ（soshiki/1/274517.html）の全リンク | 食料は「提供する側」向け2件のみ。トイレ・し尿は0件。乳幼児はこども総合療育センター外来と八代児相の執務場所変更のみ |
| 県「災害救助法の適用」（soshiki/27/274494.html） | 適用市町村の一覧のみ。炊き出し・食品給与の実施場所は無い |
| 熊本市5区の「健康・福祉」カテゴリ（chuo/list00145・higashi/list00205・nishi/list00275・minami/list00335・kita/list00415） | 5区とも地震・物資・配布・乳幼児・トイレの項目0件 |
| 熊本市の集約ページ全42件（ページ送りの取得まで） | 受け取る側向けの配布記事なし。新着は「災害見舞金等の支給」「臨時窓口開庁」（金銭・窓口の案内） |
| 上下水道局の緊急情報21件（第21報まで） | 応急給水所・断水・濁水のみ。し尿・携帯トイレ・マンホール・物資・食料・ミルク・おむつは0件 |

#### 2026-08-01 14:02 JST の追加確認（No-Go #1）［高］

| あたった先 | 結果 |
|---|---|
| 熊本県「防災情報くまもと」ポータル | 現在の避難所・道路情報と平時の防災ハンドブックを確認。被災者が受け取る食料・生活用品、トイレ、乳幼児用品の場所・時間は確認できなかった。ポータルは画面構成が動的なため、未表示の情報が無いことまでは断定しない |
| 熊本市「窓口開庁について」 | `list04832.html` は窓口開庁の分類ページで、配布先・物資・トイレ・乳幼児の個別案内は確認できなかった |
| 熊本市「義援物資について」（kiji00372129） | 個人からの義援物資は受け入れず、企業・団体からの提供を事前登録で受ける案内。被災者が受け取る場所・時間の案内ではない |
| 熊本市上下水道局トップ | 「災害対応中のため緊急情報のみ掲載しています」が継続。通常記事の復活は確認できない |

上記は受け取る側の3カードを埋める情報ではない。新しい公式発表が出るまで、同じ導線の再探索は行わない。

**2026-08-01 09:30 JST の巡回**。判定は手順単位。カードは4件増えて18件になった。

| 結果 | 件数 | カード |
|---|---|---|
| **掲載継続**（手順もそのまま実行できる） | 8 | official-kumamoto / kumamoto-city-hub / fuel / elder-care / shelter / medical / communication / roads |
| **新規追加** | 4 | food-hikawa / shelter-hikawa / bath-kumamoto / toilet-container |
| **出典は維持し、手順・説明を実態へ修正** | 3 | evidence / water / support-systems |
| **`unavailable`**（うち infant-care は手順も修正） | 3 | food-and-supplies / toilet / infant-care |

#### 取得と時刻設定の間が空いたときの扱い［高］

この回は調査の待ち時間で取得から9時間空いた。**9時間前に取った内容で `PATROL_AT` を
設定するのは鮮度の捏造にあたる**ため、巡回の最後に全URLを取り直して差分を確認し、
取り直した時刻を `PATROL_AT` にした。差分は3件（熊本市の集約ページと防災トップが
8月1日更新へ、氷川町のごみ収集が更新）で、氷川町の配布・避難所の掲載は変わっていなかった。

**取得から時刻設定までに間が空いたら、必ず取り直す。**

#### 今回の発見（No-Go #1 に直結）［高］

**氷川町の緊急情報に「8月1日（土曜日）の水・食料品の配布について」がある**
（2026-07-31 19:00 更新。9:00〜11:00 と 15:00〜17:00、竜北西部小学校・竜北東小学校・
氷川中学校）。No-Go #1 で探し続けていた「受け取る側の場所と時間」が公式に出ている唯一の例。
同ページには開設中の避難所（同じ3か所）も出ている。

**熊本市には今回も無い。** 冊子PDFの目次まで当たった結果、生活面への支援に炊き出し・
食品の給与にあたる項目そのものが無く、「4-1-1 被服、寝具その他生活必需品の支給」は
「申請受付の準備ができ次第、お知らせします」で受付前、支給品一覧も
「※以下は、令和7年8月豪雨災害時の内容です」と明記されていた。

#### 出典を超えていた表示（今回見つけたもの）［高］

| カード | 出典に無かったもの | 処置 |
|---|---|---|
| evidence | 「建物の外側4方向を撮影する」という撮り方 | 出典の記載（「家の外と中」「部屋ごとの全景」「被災箇所の寄り」）へ書き直した |
| water | （逆に取りこぼし）「飲料用」でも災害後の水質検査次第で飲用不可になりうること、井戸を管理する企業へ直接電話しないこと | verifyPoints と caution へ追加した |
| infant-care | 公式ページで開園状況が分かるかのような手順 | 出典が7月30日分で止まっているため「通っている園へ直接確認する」へ |

#### 18:30 の巡回でも取り切れていなかったこと［高］

18:30 の巡回は「そのページに**カードの話題**があるか」までは見たが、
「**カードの手順が約束していること**がそのページでできるか」までは見ていなかった。
20:16 に手順1つずつを突き合わせた結果、掲載継続としていた8件のうち4件が該当した。

| カード | 実行できなかった手順 | 出典に無かったもの |
|---|---|---|
| medical | 公式の医療情報で診療状況を確認する | 診療可能な医療機関の一覧・導線 |
| shelter | ペット・車中泊・車椅子などの条件を確認する | 避難所ごとの利用条件 |
| communication | 携帯会社の障害情報を確認する／00000JAPANや公衆電話を試す | 障害情報・00000JAPAN・公衆電話の記載 |
| roads | 公共交通の運行を確認する | 公共交通への導線 |

いずれも出典は維持できる内容を持っていたため、**手順と説明の側を出典に合わせて書き直した**
（roads だけは、地震対応の本文ページへ導線を寄せた）。
`shelter` は説明文が「ペット同伴、車中泊、車椅子、乳幼児、充電などの利用条件を確認します」と、
出典に無い条件一覧を約束していた。これは `unavailable` 表示より危険な誤認である。

#### 解消した1件

- **fuel**: 資源エネルギー庁「住民拠点SS等検索」（`enecho-ss.meti.go.jp`）へ再選定。
  震度5強以上の地震発生時に給油所の営業状況を都道府県単位で地図表示する公式システムで、
  令和8年熊本地震（震度7）が「現在の災害情報」として稼働中であることを確認した。
  **営業可／営業不可／確認中**の3区分は出典が実際に使っている区分なので `verifyPoints` にした。
  「確認中」を「営業している」と読み替えると、動かせる残量を使って空振りする。
  出典自身も「在庫不足による営業停止が発生する等、表示される営業状況と実際の状況に相違が
  生じる可能性があります」と明記している

#### `unavailable` のまま残る3件と、当たった先

- **food-and-supplies**: 見つかる案内はすべて「渡す側」向けで、「受け取る側」の場所と時間が
  公式に無い。熊本市「義援物資について」は「個人の皆様からの義援物資の受入れは行っておりません」
  と明記。県の「避難所へ食料を提供される方々へ」「被災地で炊き出しボランティアをされる方へ」は
  提供する側への衛生上の依頼。熊本市「避難所運営」は男女共同参画の視点からの運営方針で、
  食料・水・物資の提供に関する記載は無い
- **toilet**: 熊本市上下水道局の緊急情報20件（断水・応急給水所・濁水）を実際に取得したが、
  し尿・携帯トイレ・仮設トイレは0件。濁水時の「トイレ、清掃などの生活用水にはご利用いただけます」
  は水の用途の話で、使用後の処分方法ではない。マンホールトイレ一覧などの通常記事は
  災害対応中で閉じられており404。熊本市「災害(水害)し尿の収集」は制度も連絡先も実在するが、
  対象は「大雨や津波等の災害によって浸水し」た場合で、地震・断水に使えるとは書かれていない。
  **書かれていない適用範囲を推測で広げない**
- **infant-care**: 再選定先の保育施設ページで確認できるのは開園状況まで。県の令和8年熊本地震
  ページの「医療・福祉」にも乳幼児向けの配布・授乳場所の項目は無い。熊本市で見つかる
  乳幼児の相談窓口（こんにちは赤ちゃん事業・にんしんSOS熊本）はいずれも平常時の相談事業で、
  今回の地震に向けた案内ではない

#### 2026-08-01 に追加であたった先（すべて該当なし。同じ場所を再探索しない）［高］

| あたった先 | 結果 |
|---|---|
| 熊本市「被災者支援制度（冊子）」の全体版PDF（kiji00372110） | 生活必需品の支給は受付前。支給品一覧は前回災害時の内容。炊き出し・食品の給与にあたる項目が目次に無い |
| 熊本市の5区役所（中央・東・西・南・北） | 区独自の案内は無く、5区とも同一記事（kiji00372108、窓口対応）へ着地する |
| 熊本市上下水道局 `list.php` の緊急情報20件 | 断水・濁水・応急給水所のみ。「し尿」「携帯トイレ」「マンホール」「物資」「食料」「ミルク」「おむつ」はいずれも0件 |
| 熊本市上下水道局のマンホールトイレ・応急給水の通常記事 | 404 のまま復活していない |
| 宇城市 | 3テーマとも該当なし。国交省が発表したコンテナ型トイレ（小川町総合文化センター）の掲載も市側には無い |
| 宇土市 | 3テーマとも該当なし。サイト内検索は Google カスタム検索でJS必須のため、防災情報ハブの記事一覧で代替した |
| 八代市 | 給水情報はあるが食料・生活用品の配布は無し。支援物資の記事は送る側向け。乳幼児は健診中止の告知のみ |
| 氷川町 | **食料配布と開設中の避難所が該当あり**（上記「今回の発見」）。トイレ・乳幼児は該当なし |

#### `toilet` について、今回見つけたが熊本市の空白を埋めないもの［高］

国土交通省九州地方整備局が、防災用コンテナ型トイレを**美里町・宇城市・氷川町・八代市**へ
派遣したと発表している（令和8年7月30日、設置場所つき）。これは `toilet-container` カードとして
追加したが、**熊本市は派遣先に含まれない**ため `toilet` カード（熊本市）の `unavailable` は
これでは解消しない。また記者発表は「派遣した」という一時点の記録で、日々更新される
稼働状況の一覧ではない。「派遣した」と「今も使える」は別なので、カードには設置場所を出したうえで
向かう前に市町へ確認する手順を入れている。

#### 巡回対象URL［高］

**重複を除いて17件**（`list04828.html` を2カードが、氷川町の `kinkyu.html` を2カードが共有）。
内訳は県3・熊本市8・氷川町1・TCA 1・国交省九州地整 2（地震対応ページ＋コンテナ型トイレのPDF）・
資源エネルギー庁1・熊本市上下水道局1。2026-08-01 昼の巡回で上下水道局のトップ
（応急給水所カードの出典）が増えた。上下水道局は表示実体である `list.php` の JSON も
同時に取得して確認する（`X-Requested-With: XMLHttpRequest` と `Referer` が必要）。
※09:30 時点の記載「14件」は内訳（15件）と食い違っていた。データ内の sourceUrl の
重複除去で数え直した実測は、09:30 時点で15件、11:25 時点で16件。

**2026-08-01 13:00 の変更**: `support-systems` の出典を集約ページ（`list04828.html`）から
「住家の緊急の修理」本文ページ（`kiji00372143`）へ寄せたため、対象は16件から17件になった
（`list04828.html` は `kumamoto-city-hub` と `food-and-supplies` の2カードが引き続き使う）。
理由は `docs/qa/link-depth-2026-08-01/README.md` の実測: 集約ページは一覧が JS の
「もっと見る」でページ送りされるため、**開いた最初の状態では「住家の緊急の修理」という
文字列が DOM に存在しない**。カードが約束した情報へ到達できない出典だった。
深い個別記事は災害対応中に404になりやすいので、`kiji00372143` は毎日の死活確認に含める。

熊本市の3カード（食料・生活用品／トイレ／乳幼児）の公式案内欠落は、**解消していない**。
氷川町で見つかったのは食料と避難所のみ。2026-08-01 のスコープ改訂でWeb公開のブロッカーからは
外れており（`sourceStatus: "unavailable"` の正直表示のまま公開してよい）、残リスクの扱いは
`docs/RELEASE_AUDIT.md`「Web公開のブロッカーから外したもの」1 を参照する（旧「README.md の No-Go #1」）。
残るカードも24時間期限のため、**毎日1回（9:00〜21:00 JST 内）の再確認巡回が必須**。

### 巡回時に書き換える場所［高］

`lib/disaster-data.ts` の冒頭にある**2つの定数だけ**を書き換える。

```ts
const PATROL_AT = "2026-08-01T11:25:00+09:00";
const PATROL_EXPIRES_AT = "2026-08-02T11:25:00+09:00";  // PATROL_AT + 24時間
```

各カードは `...patrolled(publishedAt)` または `...patrolled(publishedAt, "unavailable")` で
この2つを共有する。`publishedAt` は出典ページに表示されている最終更新日で、
日付しか出ていないページは 00:00 へ切り下げる（鮮度を実際より新しく見せないための保守側の値）。
ページが「2026.7.31 18時00分現在」のように時刻まで書いている場合は、その時刻を採る。

**例外（当日限りの告知）**: 当日限りの告知を載せているカードは `patrolled()` の後で
`expiresAt` を上書きし、共通の24時間ではなく**その告知が対象とする日の終了時刻**で失効させる。
日付ごとに告知が出し直されるため、翌朝まで前日の時間を出し続けると、
閉まっている時間・終了した場所に人を向かわせる。

```ts
// water-station（熊本市の応急給水。告知が対象とする日の給水終了時刻）
...patrolled("2026-08-04T18:15:00+09:00"),
expiresAt: "2026-08-05T19:00:00+09:00",
```

**毎日の巡回では、この種のカードの時間・場所・`expiresAt` も最新の告知へ更新する。**

**カードを名指しで固定しない［高］**: 2026-08-01 時点では `food-hikawa` と `water-station` の
2枚がこれに当たったが、**当日限りカードの顔ぶれは出典側の都合で入れ替わる**。
2026-08-04 の巡回では氷川町の配布告知が出典から消えて `food-hikawa` が `unavailable` へ落ち、
該当は `water-station` の1枚だけになった。逆に、消えた告知の内容をカードに残すことこそが
「終わった配布へ人を向かわせる」失敗そのものなので、消えたら落とすのが正しい。
判定は毎回データから読む（`facts` に `dated: true` があるか）。

#### 受付時間帯（`availableWindows`）も同時に更新する［高］

当日限りのカードは `expiresAt` とは**別の軸**として、その日の受付時間帯を持つ。`expiresAt` は「この情報をいつまで信じてよいか」、`availableWindows` は「行けば受け取れる時間はいつか」で、両方を持たないと期限内なのに現地は閉まっている時間帯に人を向かわせる。

```ts
// 1日2回で、間に4時間の空きがある告知（2026-08-01 の氷川町の配布が実例）
availableWindows: [
  { label: "第1回", start: "2026-08-01T09:00:00+09:00", end: "2026-08-01T11:00:00+09:00" },
  { label: "第2回", start: "2026-08-01T15:00:00+09:00", end: "2026-08-01T17:00:00+09:00" },
],

// 途切れない1枠の告知（応急給水）
availableWindows: [
  { label: "応急給水", start: "2026-08-01T08:00:00+09:00", end: "2026-08-01T19:00:00+09:00" },
],
```

**入れてよいのは出典の告知に実際に書かれている時刻だけ。** 「毎日8時から開くはず」のような日付非依存の一般化もしない（告知は当日限りのため）。

巡回で守る制約は次の3つで、`tests/data-contract.test.mjs` が機械的に固定している。**片方だけ更新すると `npm test` が落ちる。**

| 制約 | 落ちる理由 |
|---|---|
| **最終枠の `end` ＝ `expiresAt`** | 当日限りカードの失効は最終受付の終了と同じ。ずれると「期限内なのに受付は終わっている」時間が生まれる |
| 全枠が `checkedAt` と同じ日（JST） | 前日の枠が残っていると、終わった時間帯を今日の予定として出す |
| 各枠 `start < end`／時刻順で重ならない | 重なった枠は「いまは受付時間外」の判定を壊す |

`availableWindows` を持つカードは `caution` に「出発前」を含める（同テストが固定）。表示は正典の `serviceWindowNotice()` が作るので、文言を画面側で書かない。受付時間外は言い切り、受付時間内は「告知では」を付けて中止・早期終了の可能性を必ず添える（断定の方向が非対称）。

**当日限りの告知を持たないカードへ `availableWindows` を足さない。** 出典に受付時間の告知が無いカードへ推測で足すことになる。

**告知の対象日が巡回日と違うときは枠を置かない［高］**（2026-08-04 21:02 の巡回で判明）。
枠は `checkedAt` と同じ日付でなければならない（上表の制約）。夜の巡回で最新の告知が
**翌日分**だった場合、当日の枠を作れば出典に無い時刻の捏造になり、翌日の枠を入れれば
`serviceWindowNotice()` が明日の予定を「本日」として出す。どちらも避け、
**`availableWindows` を外して時刻は `facts` だけで伝える**。
告知の対象日が巡回日と同じ日になったら、次の巡回で枠を戻す。

同じ理由で、**日付を持たない常設の告知にも枠を置かない**（氷川町の給水車「時間：7：00〜19：00」など）。
枠にすると「毎日7時から開くはず」という出典に無い一般化になる。

### カード内の答え（`facts`）の照合［高］

2026-08-01 13:00 から、出典に書かれている核心情報（場所・時間・住所・電話番号）を
`facts` としてカード内に出している。**巡回では `facts` の各行を出典の本文と1行ずつ
突き合わせる**。カードに答えを出すということは、出典の記載を写した箇所が増えるという
ことであり、写した箇所はすべて劣化する。

最初に機械照合を実行する。20カードの `sourceUrl` を17 URLへ重複排除して取得し、
`facts.items` / `facts.citedAs` / `sourceLandmark` の不一致と、期限切れの `dated: true` を表示する。
上下水道局はトップページの到達確認に加え、`list.php` を必要なヘッダー付きで取得する。

```bash
npm run patrol:diff
```

- 終了コード `0`: 取得成功・引用文字列の一致。`[到達]` のカードは引用文字列を持たないため、
  下記の手順単位の人間確認が別途必要
- 終了コード `1`: `[要確認]` あり。公式本文を読み、出典から消えた行はカードからも消す
- 終了コード `2`: `[取得失敗]` あり。巡回成功として扱わず、ネットワークまたは対象URLを確認する
- `--json` で機械可読出力、`--timeout-ms <整数>` で1リクエストの上限時間を指定できる

このコマンドは `PATROL_AT` / `PATROL_EXPIRES_AT` やカード本文を自動更新しない。
HTTP 200や文字列一致を、手順が実行可能であることや発表日の確認へ読み替えない。

```text
1. カードの facts.citedAs に書いた見出しが、出典ページに今も存在するか
   → 無い : 見出しが変わった。citedAs と sourceLandmark を実物の文言へ直す
2. facts.items の各行が、その見出しの下に今も書かれているか
   → 違う : 出典の記載へ書き換える。**出典から消えた行はカードからも消す**
3. dated: true の facts は、当日分の内容へ入れ替える
   （water-station の給水所12か所、food-hikawa の配布時間、evidence の臨時窓口）
```

`dated: true` を付けた `facts` は `expiresAt` を過ぎると表示から消える（`visibleFacts()`）。
これは「昨日の給水所へ人を向かわせない」ための安全弁であって、**更新の代わりではない**。
電話番号のように日付へ依存しない答えは `dated` を付けず、期限切れでも出し続ける
（情報が古いときほど「どこへ聞けばよいか」が要るため）。

`sourceLandmark`（開いたページで探す先の名指し）も同じ扱いで、
**出典の実物にある文言だけ**を入れ、巡回のたびに存在を照合する。

書き換えたら次を実行する。

```bash
npm run gen:mobile-data && npm test
```

- `expiresAt` は `checkedAt` の**24時間後**（旧: 4時間後。2026-07-31 ユーザー確定、下記「採用」を参照）

### 論点（採用済み／`[提案]`）

4時間という設定は「災害時の情報は4時間で古くなる」という判断としては妥当だが、
**巡回できる頻度と釣り合っていない**。1人運用で4時間ごとに14件を再確認するのは持続しない。
選べる方向として3つを検討した。

1. **カテゴリ単位で期限を分ける** — 「熊本県の公式ページを開く」のような導線カードは
   1日単位でも実質劣化しない。一方「今日の給水地点」は4時間でも長い。
   現在は全件が同じ値を共有しているので、劣化速度の違いを表現できていない
2. **`checkedAt` だけを更新する軽い確認ルーチン** — 「リンク先が生きていて、
   掲載内容が変わっていない」ことだけを確認して時刻を延ばす。中身の再確認より軽い。
   ただし**リンクが生きていることと内容が正しいことは別**なので、
   これで延ばしてよいのは導線カードだけ
3. **巡回頻度に期限を合わせる** — 1日1回しか見られないなら `expiresAt` も1日にする。
   ただし**期限を延ばすこと自体は情報を新しくしない**。「4時間で失効するが誰も更新していない」
   状態と「24時間有効ということにしている」状態では、後者の方が危険

**採用（2026-07-31 ユーザー確定）**: 提案1＋2を採用し、期限を**24時間**とした。
本アプリの全カードは公式サイトへの導線カードであり、1人運用の1日1回巡回の現実的な頻度と
釣り合わせるための判断。提案3（巡回頻度に期限を合わせるだけ）単独は採用していない。
期限を延ばすこと自体は情報を新しくしないため、
**24時間ごとの再確認巡回を止めた場合は、この判断の前提が崩れる**。

**提案2の「軽い確認」の中身を、同日 18:30 の巡回結果を受けて修正した**。
当初は「リンク先が生きていて、掲載内容が変わっていない」ことだけを見る想定だったが、
これでは**リンクが生きたまま中身だけ話題を失う壊れ方を検出できない**ことが実地で判明した
（14カード中6カードが該当）。確認の単位は「リンクが生きているか」ではなく
「**カードの steps に書いた行動がリンク先で実行できるか**」とする（上記「巡回の判定ルール」）。
リンク生存だけで `checkedAt` を延ばすことは、実質的に鮮度の捏造にあたるため行わない。

**同日 20:16 の巡回で、この修正でもまだ緩かったことが分かった**。18:30 の確認は
「そのページにカードの話題があるか」で止まっており、「カードの手順が約束していることが
できるか」までは見ていなかった。手順1つずつを突き合わせたところ、掲載継続としていた8件のうち
4件（medical / shelter / communication / roads）で手順が実行できなかった。
**確認は手順単位で行う**。カードの話題がページにあることは、手順が実行できることを意味しない。

**やってはいけないこと**: 公式ページを再確認せずに `expiresAt` だけ延ばす。
これは鮮度の捏造であり、このアプリが唯一守ると約束した品質基準に反する。

### 巡回を続けられなくなったとき［高］

24時間期限の採用は毎日巡回の継続が前提（上記「論点」）。1人運用のため、体調・機材・
長期不在で巡回が止まる事態は想定内として、先に手順を決めておく。

前提として、**巡回が止まってもアプリは嘘をつかない**。全カードは `expiresAt` 超過で
自動的に「現在の状況は確認できません」表示へ縮退し、これは端末の時計で動くため
オフラインでもネイティブでも効く（3.5節）。巡回停止の最悪結果は
「全カードが未確認表示になり、119/110と公式サイト導線だけが残る」であって、
「古い情報が新しい顔で出続ける」ではない。

手順：

1. 次の巡回（24時間以内）に間に合わないと分かった時点で、可能なら
   `npx wrangler secret put EMERGENCY_MODE`（値 `true`）で縮退モードを立てる。
   オンライン利用者には数分で届き、失効を待つより早い（3.2節）。
2. それも実行できない状況なら、何もしなくてよい。失効表示が唯一の自動的な
   劣化通知として機能する（3.5節）。
3. 巡回を再開するときは、`checkedAt` を機械的に更新せず、全カードを判定ルール
   （手順単位。上記「巡回の判定ルール」）で再確認してから時刻を更新する。
   `npm run patrol:diff` の差分を先に見ると、読む範囲を絞れる。

**やってはいけないこと**：巡回できないことを理由に `expiresAt` を延ばす。
巡回が止まった事実を隠して公開を続けることと同じであり、鮮度の捏造にあたる。

---

## 6. 公開判定チェックリスト（No-Go #2）

No-Go #2 を `verified` にするために必要なもの。

| # | 条件 | 検証方法 | 状態 |
|---|---|---|---|
| 1 | 停止スイッチがコードとして正しく動く | `npm test`（`tests/emergency-mode.test.mjs` 8件） | **済**［高］ |
| 2 | 同一ビルドのまま環境変数だけで切り替わる | `docs/qa/emergency-mode-2026-07-31/` の実測 | **済**［高］ |
| 3 | hydration 後もカードが復活しない | 実ブラウザの実DOM計測（同上） | **済**［高］ |
| 4 | workerd（本番と同じランタイム）で bindings から届く | `.dev.vars` 経由の実測（同上） | **済**［高］ |
| 5 | **本番 Workers で実操作し、反映までの実時間を測る** | secret を設定 → 別端末で `/` と `/status` を確認 → 解除 | **未**（デプロイ承認後） |
| 6 | **停止中にデプロイしても停止が維持される** | 停止中に無害な変更をデプロイし、`/status` が「緊急縮退中」のままか確認 | **未** |
| 7 | **問い合わせ窓口の URL が実在する** | remote を設定し、Issues が開けること | **済**［高］https://github.com/House-lovers7/save_kumamoto/issues（2026-08-01 開設） |
| 8 | **停止判断者が実際に手順を実行できる** | 運営者本人が 3.2 を1回通す | **未** |
| 9 | **ネイティブに停止が届かないことを受諾する** | 運営者の明示的な同意 | **未** |

1〜4 が済んでも判定は `implementation_complete_boundary_unverified` のまま。
localhost と miniflare で採れた証拠を、本番境界の証拠に読み替えない。

---

## 7. UIへの反映（`[提案]`）

現在 `/status` には停止状態と安全上の境界しか出ていない。問い合わせ先は出していない。

- `[提案]` `/status` に「誤りを見つけたら」の1行と窓口を出す。実値は `[要記入]`
- 個人情報を晒さない設計にする。個人のメールアドレスを公開ページに直書きすると、
  永続的に収集ボットの対象になる。GitHub Issues か、専用アドレスか、フォームを使う
- **トップページ（`/`）には出さない**。被災者が最初に見る画面は「いま何をするか」であって、
  運営者への連絡ではない。119・110 の導線と competing させない

---

## 関連

- `README.md`「緊急縮退」（公開前に残る課題の全体は `docs/RELEASE_AUDIT.md` 側）
- `docs/RELEASE_AUDIT.md`「Acceptance Chain」「公開停止条件」「Rollback」「停止の到達範囲」
- `docs/qa/emergency-mode-2026-07-31/README.md`（実測証拠）
- `lib/emergency-mode.ts`（停止スイッチの唯一の実装）
- `tests/emergency-mode.test.mjs`（退行検出）
